<Window x:Class="MonLingo.Core.UI.ForgotPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重設密碼" 
        Height="280" 
        Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">
    
    <Window.Resources>
        <!-- 按鈕樣式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BD2130"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本框樣式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 主容器 -->
    <Border Background="White" 
            CornerRadius="12" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.15" ShadowDepth="5" BlurRadius="15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 標題欄 -->
            <Grid Grid.Row="0" Background="#F8F9FA" Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 標題 -->
                <TextBlock Grid.Column="0" 
                          Text="重設密碼" 
                          VerticalAlignment="Center" 
                          Margin="15,0,0,0"
                          FontWeight="Medium"
                          Foreground="#333"/>

                <!-- 關閉按鈕 -->
                <Button Grid.Column="1" 
                        Content="✕" 
                        Width="40" 
                        Height="40"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="16"
                        Foreground="#666"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FF5555"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- 主要內容 -->
            <StackPanel Grid.Row="1" Margin="30,20,30,25">

                <!-- 圖標 -->
                <TextBlock Text="🔒" 
                           FontSize="32" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,15"/>

                <!-- 說明文字 -->
                <TextBlock Text="輸入您的電子郵件地址，我們將發送密碼重設連結" 
                           TextWrapping="Wrap"
                           Foreground="#666"
                           TextAlignment="Center"
                           Margin="0,0,0,20"
                           FontSize="13"/>

                <!-- 電子郵件輸入 -->
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="電子郵件" FontWeight="Medium" Margin="0,0,0,5" Foreground="#333"/>
                    <TextBox x:Name="EmailTextBox" 
                             Style="{StaticResource ModernTextBox}"
                             Height="40"/>
                </StackPanel>

                <!-- 重設密碼按鈕 -->
                <Button x:Name="ResetButton" 
                        Content="發送重設連結" 
                        Style="{StaticResource ModernButton}"
                        Height="40"
                        Margin="0,0,0,15"
                        Click="ResetButton_Click"/>

                <!-- 返回登入 -->
                <Button Content="← 返回登入" 
                        Background="Transparent"
                        Foreground="#007ACC"
                        BorderThickness="0"
                        FontSize="13"
                        HorizontalAlignment="Center"
                        Cursor="Hand"
                        Click="BackToLogin_Click"/>

                <!-- 載入指示器 -->
                <Grid x:Name="LoadingPanel" Visibility="Collapsed" Margin="0,10,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Height="3" Width="120" Margin="0,0,0,8"/>
                        <TextBlock x:Name="LoadingText" 
                                   Text="正在發送..." 
                                   HorizontalAlignment="Center"
                                   Foreground="#666"
                                   FontSize="12"/>
                    </StackPanel>
                </Grid>

                <!-- 錯誤訊息 -->
                <Border x:Name="ErrorPanel" 
                        Background="#FFF5F5" 
                        BorderBrush="#FF6B6B" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="10"
                        Margin="0,10,0,0"
                        Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" 
                               Foreground="#D63384" 
                               TextWrapping="Wrap"
                               FontSize="12"/>
                </Border>

                <!-- 成功訊息 -->
                <Border x:Name="SuccessPanel" 
                        Background="#F0F9F0" 
                        BorderBrush="#28A745" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="10"
                        Margin="0,10,0,0"
                        Visibility="Collapsed">
                    <TextBlock x:Name="SuccessText" 
                               Foreground="#155724" 
                               TextWrapping="Wrap"
                               FontSize="12"/>
                </Border>

            </StackPanel>
        </Grid>
    </Border>
</Window>
